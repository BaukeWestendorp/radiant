[package]
name = "flow2"
version.workspace = true
edition.workspace = true

[features]
serde = ["dep:serde"]

[dependencies]
gpui.workspace = true
ui.workspace = true

# feature = "serde"
serde = { workspace = true, optional = true }
