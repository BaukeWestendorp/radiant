[package]
name = "river"
version = "0.1.0"
edition.workspace = true
license = "MIT"

[dependencies]
slotmap = "1.0"
gpui = { git = "https://github.com/zed-industries/zed", tag = "v0.133.7", optional = true }

[features]
default = ["gpui"]
gpui = ["dep:gpui"]
