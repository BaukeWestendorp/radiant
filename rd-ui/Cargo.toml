[package]
name = "rd-ui"
version.workspace = true
edition.workspace = true

[features]
derive = ["dep:rd-ui-derive"]

[dependencies]
gpui.workspace = true
gpui_platform.workspace = true
log.workspace = true
regex = "1.12.4"
smallvec = "1.15.1"
uuid = { version = "1.20.0", features = ["v4"] }

# feature = "derive"
rd-ui-derive = { workspace = true, optional = true }

[dev-dependencies]
anyhow.workspace = true
pretty_env_logger = "0.5.0"

[[examples]]
name = "preview"
features = ["derive"]
